{
  "name": "backend-napi",
  "version": "0.1.0",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "backend-napi",
    "targets": [
      "x86_64-unknown-linux-gnu"
    ]
  },
  "scripts": {
    "build": "napi build --release && [ -f index.node ] && mv index.node index.linux-x64-gnu.node || true",
    "build:debug": "napi build && [ -f index.node ] && mv index.node index.linux-x64-gnu.node || true"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4"
  }
}
