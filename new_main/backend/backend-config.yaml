# Logging
# Levels: error | warn | info | debug
# Use 'debug' for verbose GCP debugging, 'warn' or 'error' for quiet production.
LOG_LEVEL: "debug"

# Server Configuration
APP_URL: "https://dedalthegame.com"
GCS_BUCKET: "data-bucket-mylittleproject00"

# JWT Authentication (HS256)
JWT_SECRET: "d4e8f1a2b3c5d6e7f8a9b0c1d2e3f4a5b6c7d8e9f0a1b2c3d4e5f6a7b8c9d0e1"
JWT_EXPIRY_DAYS: "7"

# Email Configuration (Nodemailer)
EMAIL_SERVICE: "gmail"
EMAIL_USER: "iiiiiiiiiiii@gmail.com"
EMAIL_PASS: "iiii iiii iiii iiii"

# Bot Configuration
BOT_SERVER_URL: "http://localhost:5001"
BOT_POLL_INTERVAL_MS: "120000"
BOT_MOVE_DELAY_MS: "500"
MCTS_DEFAULT_BUDGET_MS: "500"

# Background Bot Matches
# BOT_MATCH_INTERVAL_MS: "3600000"
# BOT_MATCH_PROBABILITY: "0.0001"
BOT_MATCH_INTERVAL_MS: "3600000"
BOT_MATCH_PROBABILITY: "0.01"

# Tournament Configuration
TOURNAMENTS_ENABLED: "true"
MAX_CONCURRENT_TOURNAMENTS: "20"
MAX_BOTS_IN_TOURNAMENTS: "20"
TOURNAMENT_WIN_POINTS: "1"
TOURNAMENT_DRAW_POINTS: "0.5"
TOURNAMENT_LOSS_POINTS: "0"
TOURNAMENT_MAX_AGE_HOURS: "24"
TOURNAMENT_OPEN_EXPIRY_HOURS: "24"

# Valkey (Redis-compatible) backplane for multi-instance scaling.
# Set VALKEY_ENABLED to "true" when a Valkey/Redis instance is available.
# With "false" the server runs in single-instance mode (no retries, no adapter).
VALKEY_ENABLED: "true"
VALKEY_HOST: "127.0.0.1"
VALKEY_PORT: "6379"

# PostgreSQL
PG_HOST: "127.0.0.1"
PG_PORT: "5432"
PG_DATABASE: "dedalthegame01"
PG_USER: "tamias23"
PG_PASSWORD: "TY-rre__U@345"

# Game History Offload
# Games older than GAME_RETENTION_DAYS are exported to /mnt/db as Parquet files.
# Scheduling is controlled by the cron runner (parquet_export job, default 20:00 UTC).
GAME_RETENTION_DAYS: "90"
SHOW_SUBSCRIBE_BUTTON: "false"
